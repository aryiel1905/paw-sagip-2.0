/** How long (ms) to wait before playing another alert notification sound */
export const ALERT_NOTIFY_COOLDOWN_MS = 2500;

/** Default number of alerts/adoptions per page in paginated views */
export const DEFAULT_PAGE_SIZE = 60;

/** Maximum page size allowed by the adoptions API */
export const MAX_PAGE_SIZE = 200;
